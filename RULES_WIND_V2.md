# 风属性系统规则文档 (Wind System Rules) v2.5

本文档详细介绍了 `Echo Alchemist` 中风属性子弹的法阵触发机制、伤害公式及视觉表现规则。

## 1. 核心机制：锚点 (Anchors)

风属性子弹击中目标或墙壁时会生成一个**风之锚点**。
- **最大数量**：场景中最多同时存在 4 个锚点。
- **自动替换**：当生成第 5 个锚点时，最早生成的锚点会消失，并触发一次小旋风爆炸（造成 2 倍子弹伤害）。
- **法阵触发**：当锚点数量达到 4 个时，系统会自动判定其几何形状并触发相应的**风之法阵**。

## 2. 法阵类型与触发条件

系统根据 4 个锚点构成的几何特征，按优先级判定触发类型：

| 优先级 | 法阵类型 | 几何判定条件 | 视觉表现 |
| :--- | :--- | :--- | :--- |
| 1 | **蝴蝶法阵 (Butterfly)** | 4 个锚点构成的四边形存在**自相交**（交叉形）。 | 触发时显示路径波预览，随后从屏幕外飞入大量风刃。 |
| 2 | **风道 (Wind Tunnel)** | 形状不相交，且包围盒**宽高比 ≥ 3.0**。 | 沿长轴方向的强风流光特效。 |
| 3 | **风暴核心 (Storm Core)** | 形状不相交，且包围盒**面积 < 6000**。 | 动态透明度核心，子弹进入时变亮并充能。 |
| 4 | **暴风绞杀 (Strangle)** | 默认情况（形状较大且相对方正）。 | 大量风刃向中心旋转坍塌。 |

## 3. 伤害公式重构 (v2.5)

对于**暴风绞杀**和**风道**，伤害逻辑已重构为多段打击模式，深度关联子弹属性：

### 3.1 多段伤害公式
- **总打击次数 (Tick Count)** = `1 + scatter` (散射属性层数)
- **单次打击伤害 (Tick Damage)** = `基础伤害 * (1 + multicast) / (1 + scatter)`
- **打击间隔**：每段伤害间隔为 100ms。

### 3.2 属性联动加成
- **火旋风联动**：若敌人处于燃烧状态，单次打击伤害额外 **x2**。
- **冰旋风联动**：若敌人处于冰冻状态，单次打击伤害额外 **x2**。
- **黑洞进化**：当风属性与激光 (Laser) 属性结合时，暴风绞杀进化为黑洞，造成即死级伤害 (9999)。

## 4. 视觉交互规范

- **路径波预览**：蝴蝶法阵触发瞬间，会从锚点向中心发射流动的虚线粒子。
- **动态核心**：风暴核心平时保持 **30% 透明度**，仅在子弹进入半径内时平滑过渡至 **100% 透明度**。
- **独立 ShotId**：每一段打击都拥有独立的 `shotId` 标识，确保伤害数字清晰弹出且不被系统过滤。

---
*此文档随代码逻辑同步更新，旨在为开发者和 AI Agent 提供准确的机制索引。*

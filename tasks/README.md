# Tasks Archive Pointer

Historical task folders were archived to `docs/archive/tasks/`.

Keep new active task notes lightweight here only when they are needed for current work. Move completed or obsolete task records back into `docs/archive/tasks/` so agents do not load old delivery logs by default.

# P0 交互优化 TODO（2026-06-18）

本文档用于承接“先把现有机制打磨好，bug 解决好”的当前方向：在继续做减法或追加大功能前，优先补齐核心页面的可理解性、可操作性和反馈闭环。

## 1. 优先级总览

| 优先级 | 页面 / 系统 | 当前状态 | 下一步判断 |
| :--- | :--- | :--- | :--- |
| P0 | 弹珠选择 / 命运时刻（`#phase-selection`） | 已完成首轮交互修正 | 后续只补视觉层级与极端状态校验 |
| P0 | 钉板收集 / 模块编辑（`#phase-gathering` + `#module-editor-layer`） | 已完成首轮交互修正 | 后续补编辑态视觉、融合预览与错误提示一致性 |
| P0 | 战斗主界面（`#phase-combat`） | 下一轮优先处理 | 先处理首屏层级、危险反馈、护盾/生命线清晰度 |
| P0 | 敌人视觉系统 | 下一轮优先处理 | 先处理碰撞边界、状态 Overlay、Boss/精英层级 |
| P1 | 符文发射器（`#phase-rune-launcher`） | 待梳理 | 与钉板符文融合建立明确关系 |
| P1 | 遗物 / 商店 / 真理之书 | 待梳理 | 统一奖励、购买、图鉴的卡片语法 |

## 2. 已落地项

### 2.1 弹珠选择 / 命运时刻

- [x] 子弹替换流程补齐选择数量、禁用原因和上限提示。
- [x] 纯净精华替换路径修复：在没有可替换弹药时能给出合理 fallback。
- [x] 卡片支持键盘焦点、回车/空格确认与基础 ARIA 语义。
- [x] 顶部状态栏加入护盾显示，避免临时护盾只存在于数值层。
- [x] 回合奖励队列和高压提示已接入更明确的反馈入口。

### 2.2 钉板收集 / 模块编辑

- [x] 模块放置从“先清旧布局再尝试写入”改为“先预校验再提交”，避免非法操作破坏旧布局。
- [x] 模块选择浮层会显示不可用项及原因，包括越界、占用、未解锁槽位。
- [x] 模块编辑器补齐键盘逃逸和点击外部关闭的基础交互。
- [x] 为后续符文融合、模块槽视觉状态和开始采集边界预留了交互检查点。

## 3. 下一轮 P0 TODO

### 3.1 战斗主界面

- [ ] 梳理首屏视觉层级：战场、敌人、发射器、技能栏、符文充能、伤害数字的优先级。
- [ ] 固定危险反馈语法：低血量、临时护盾消耗、高压波次、Boss 入场应使用一致的提示层。
- [ ] 检查弹药与投射物反馈：当前弹药、替换后的弹药、命中/暴击/克制反馈需要更容易被玩家读到。
- [ ] 检查暂停、失败、回合结束时的状态残留，避免视觉提示跨阶段误留。

### 3.2 敌人视觉系统

- [ ] 明确敌人可碰撞边界和实际占位，尤其是多格敌人与 Boss。
- [ ] 统一血条、护盾、温度、词缀、狂暴等 Overlay 的层级与遮挡规则。
- [ ] 区分普通、精英、Boss 的第一眼层级，避免只靠尺寸或颜色猜测威胁。
- [ ] 检查大型敌人生成、碰撞、视觉足迹与文档 `docs/enemy_visual_design_v2.md` 的一致性。

## 4. 后续 P1 TODO

- [ ] 符文发射器：统一配置、管理、词条图鉴三个 Tab 的信息架构。
- [ ] 符文发射器：把钉板符文融合结果与发射器可用词条建立明确的反馈链。
- [ ] 遗物 / 商店 / 真理之书：统一卡片头部、稀有度、代价、收益、禁用原因的呈现方式。
- [ ] 训练场 / 图鉴：补充核心机制的可验证入口，降低后续调参的试错成本。

## 5. 验收清单

涉及代码时至少执行：

- `node --check src/ui_system.js`
- `node tests\validate_scenarios.js`

如果修改已索引大文件，按 `AGENTS.md` 要求更新函数索引，例如：

```bash
python scripts/generate_index.py . --file src/ui_system.js
```

如果修改粒子、特效、敌人绘制、Canvas 混合模式或发光效果，必须先读 `.cursor/rules/performance.md`，并按性能自适应影响标记规范补 `// @perf-impact` 与总结说明。

## 6. 工作边界

- 先修现有机制与体验断点，再讨论删减或新增系统。
- 不做核心大文件全量重写；超过局部修改范围时先拆模块或补小型辅助函数。
- 交互优化必须同步更新对应索引入口，避免 TODO 散落在聊天记录里。
- 本地浏览器如果因安全策略无法打开 `localhost`，不绕过策略；改用静态检查、单元验证或等待用户批准。
